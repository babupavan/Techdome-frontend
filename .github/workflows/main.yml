name: Build, Push, and Run with Docker

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'
      - name: Install Dependencies
        run: npm install
  docker:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Build Docker Image
        run: sudo docker build -t gpavankumarreddy10/techdome:latest .
      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin
      - name: change the name
        run: docker tag gpavankumarreddy10/techdome:latest gpavankumarreddy/babu:techdomeimage1
      - name: Push Docker Image to Docker Hub
        run: docker push gpavankumarreddy/babu:techdomeimage1
      - name: Run Docker Container
        run: sudo docker run -d -p 8081:3000 gpavankumarreddy123/techdome:latest
