name: Build, Push, and Run with Docker

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install Dependencies
        run: npm install

  docker:
    needs: build
    runs-on: ubuntu-latest

 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Build Docker Image
        run: docker build -t gpavankumarreddy1/techdome:latest .

      - name: Run Docker Container
        run: docker run -d -p 8081:8080 gpavankumarreddy1/techdome:latest
